<%= form_for(@searchn_view_item) do |f| %>
  <% if @searchn_view_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@searchn_view_item.errors.count, "error") %> prohibited this searchn_view_item from being saved:</h2>

      <ul>
      <% @searchn_view_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
