<%= form_for(@my_account) do |f| %>
  <% if @my_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_account.errors.count, "error") %> prohibited this my_account from being saved:</h2>

      <ul>
      <% @my_account.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
